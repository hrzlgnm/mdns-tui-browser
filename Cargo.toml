[package]
name = "mdns-tui-browser"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
description = "A TUI browser for mDNS service discovery"

[package.metadata.deb]
maintainer = "hrzlgnm"
copyright = "Copyright 2026 hrzlgnm"
license-file = "LICENSE"
depends = "libgcc-s1 (>= 4.2)"
assets = [
    [
        "target/release/mdns-tui-browser",
        "usr/bin/",
        "755",
    ],
    [
        "docs/mdns-tui-browser.1",
        "usr/share/man/man1/mdns-tui-browser.1",
        "644",
    ],
    [
        "target/debian/changelog.gz",
        "usr/share/doc/mdns-tui-browser/changelog.gz",
        "644",
    ],
]

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
debug = true
split-debuginfo = "packed"
strip = true

[dependencies]
mdns-sd = "0.21"
tokio = { version = "1.0", features = ["full"] }
ratatui = "0.30"
crossterm = "0.29"
flume = "0.12"
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
if-addrs = "0.15"
open = "5"
url = "2"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.31", features = ["signal"] }
